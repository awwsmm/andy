{
  "name": "andy",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rimraf ./build",
    "tests": "mocha --require ts-node/register --require tsconfig-paths/register 'tests/**/*.ts'",
    "build": "tsc -p tsconfig-build.json",
    "main": "node -r ts-node/register/transpile-only -r tsconfig-paths/register build/scripts/index.js",
    "clean:build": "npm run clean && npm run build",
    "clean:tests": "npm run clean:build && npm run tests",
    "clean:main": "npm run clean:build && npm run main",
    "daemon": "nodemon"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/chai": "^4.2.21",
    "@types/mocha": "^9.0.0",
    "@types/node": "^16.4.1",
    "chai": "^4.3.4",
    "mocha": "^9.0.2",
    "nodemon": "^2.0.12",
    "rimraf": "^3.0.2",
    "ts-node": "^10.1.0",
    "tsconfig-paths": "^3.10.1",
    "tslib": "^2.3.0",
    "typescript": "^4.3.5"
  }
}
